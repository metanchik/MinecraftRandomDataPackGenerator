package com.example.examplemod

class ItemKey {
    var a: ItemKeyEntry
    var b: ItemKeyEntry
    var c: ItemKeyEntry
    var d: ItemKeyEntry
    var e: ItemKeyEntry
    var f: ItemKeyEntry
    var g: ItemKeyEntry
    var h: ItemKeyEntry
    var i: ItemKeyEntry

    constructor(aa: String, bb: String, cc: String, dd : String, ee : String, ff: String, gg : String, hh : String, ii: String) {
        a = ItemKeyEntry(aa)
        b = ItemKeyEntry(bb)
        c = ItemKeyEntry(cc)
        d = ItemKeyEntry(dd)
        e = ItemKeyEntry(ee)
        f = ItemKeyEntry(ff)
        g = ItemKeyEntry(gg)
        h = ItemKeyEntry(hh)
        i = ItemKeyEntry(ii)
    }
}